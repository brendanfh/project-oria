import {
}

Game = class {
    init = function(self)
    end;

    update = function(self)
    end;

    draw = function(self)
        love.graphics.setBackgroundColor(255, 0, 0)
    end
}

return module { Game }
